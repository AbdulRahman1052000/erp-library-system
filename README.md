# نظام إدارة المكتبة ERP 📚

## نظرة عامة
نظام إدارة مكتبة متكامل مبني على تقنية ERP (Enterprise Resource Planning) يوفر حلاً شاملاً لإدارة العمليات المختلفة داخل المكتبة من الفهرسة والإعارة والعودة والتقارير وإدارة المستخدمين.

## المميزات الرئيسية ✨

### 📖 إدارة المجموعات
- إضافة وتعديل وحذف مصادر المعلومات
- تصنيف المصادر حسب فئات مختلفة
- تتبع حالة كل مصدر (متاح، معار، تالف، مفقود)
- إدارة نسخ متعددة من نفس المصدر
- البحث والتصفية المتقدمة

### 👥 إدارة المستخدمين
- نظام تسجيل وتسجيل دخول آمن
- إدارة ملفات المستخدمين الشخصية
- نظام الأدوار والصلاحيات المتقدم
- تتبع سجل النشاط لكل مستخدم
- إدارة فترات الاشتراك والعضويات

### 📤 إدارة الإعارة والعودة
- نظام إعارة سهل وسريع
- تحديد مدة الإعارة حسب نوع المصدر
- تنبيهات تلقائية لمواعيد انتهاء الإعارة
- نظام الغرامات والتأخير
- تقارير مفصلة عن الإعارات المتأخرة

### 📊 التقارير والإحصائيات
- تقارير شاملة عن حركة المصادر
- إحصائيات عن الاستخدام والنشاط
- تقارير الأداء والكفاءة
- تصدير التقارير بصيغ متعددة (PDF, Excel)
- لوحة تحكم تفاعلية

### 🔔 الإشعارات والتنبيهات
- تنبيهات بريدية تلقائية
- تنبيهات عن انتهاء مدة الإعارة
- إشعارات عن الكتب الجديدة
- التنبيهات حسب تفضيلات المستخدم

## المتطلبات التقنية 🛠️

### المتطلبات الأساسية
- **Python**: الإصدار 3.8 أو أحدث
- **Django**: 4.0 أو أحدث
- **قاعدة البيانات**: PostgreSQL 12 أو أحدث
- **Redis**: لنظام الكاش والرسائل
- **Node.js**: 14.0 أو أحدث (للواجهة الأمامية)

### المكتبات الإضافية
```
djangorestframework>=3.13.1
django-cors-headers>=3.11.0
celery>=5.2.0
python-dotenv>=0.19.0
psycopg2-binary>=2.9.0
pillow>=9.0.0
reportlab>=3.6.0
pandas>=1.3.0
```

## التثبيت والإعداد 🚀

### 1. استنساخ المستودع
```bash
git clone https://github.com/AbdulRahman1052000/erp-library-system.git
cd erp-library-system
```

### 2. إنشاء بيئة افتراضية
```bash
python -m venv venv
source venv/bin/activate  # على Windows: venv\Scripts\activate
```

### 3. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 4. إعداد متغيرات البيئة
```bash
cp .env.example .env
# قم بتعديل ملف .env بالقيم الخاصة بك
```

### 5. إعداد قاعدة البيانات
```bash
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser
```

### 6. تحميل البيانات الأولية (اختياري)
```bash
python manage.py loaddata initial_data.json
```

### 7. تشغيل خادم التطوير
```bash
python manage.py runserver
```

الآن يمكنك الوصول للنظام على: `http://localhost:8000`

## هيكل المشروع 📁

```
erp-library-system/
├── manage.py
├── requirements.txt
├── .env.example
├── README.md
├── apps/
│   ├── users/                    # تطبيق إدارة المستخدمين
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   ├── urls.py
│   │   └── tests.py
│   ├── resources/                # تطبيق إدارة المصادر
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   ├── urls.py
│   │   └── tests.py
│   ├── borrowing/                # تطبيق الإعارة والعودة
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   ├── urls.py
│   │   └── tests.py
│   ├── reports/                  # تطبيق التقارير
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   ├── urls.py
│   │   └── tests.py
│   └── notifications/            # تطبيق الإشعارات
│       ├── models.py
│       ├── views.py
│       ├── tasks.py
│       └── urls.py
├── config/                       # إعدادات المشروع الرئيسية
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── static/                       # الملفات الثابتة
├── media/                        # الملفات المرفوعة
└── tests/                        # اختبارات شاملة
```

## الاستخدام الأساسي 💡

### إضافة كتاب جديد
```python
from apps.resources.models import Resource

Resource.objects.create(
    title="عنوان الكتاب",
    author="اسم المؤلف",
    isbn="978-3-16-148410-0",
    category="العلوم",
    publication_year=2023,
    total_copies=5
)
```

### إعارة مصدر
```python
from apps.borrowing.models import BorrowingRecord
from django.utils import timezone
from datetime import timedelta

record = BorrowingRecord.objects.create(
    user=user,
    resource=resource,
    borrow_date=timezone.now(),
    due_date=timezone.now() + timedelta(days=14),
    status='active'
)
```

### إرجاع مصدر
```python
record.return_date = timezone.now()
record.status = 'returned'
record.save()
```

## واجهة برمجية التطبيقات (API) 🔌

### النقاط الأساسية (Endpoints)

#### المستخدمين
- `GET /api/users/` - قائمة المستخدمين
- `POST /api/users/` - إنشاء مستخدم جديد
- `GET /api/users/{id}/` - تفاصيل مستخدم
- `PUT /api/users/{id}/` - تحديث بيانات مستخدم
- `DELETE /api/users/{id}/` - حذف مستخدم

#### المصادر
- `GET /api/resources/` - قائمة المصادر
- `POST /api/resources/` - إضافة مصدر جديد
- `GET /api/resources/{id}/` - تفاصيل مصدر
- `PUT /api/resources/{id}/` - تحديث مصدر
- `DELETE /api/resources/{id}/` - حذف مصدر

#### الإعارات
- `GET /api/borrowing/` - قائمة الإعارات
- `POST /api/borrowing/` - إنشاء إعارة جديدة
- `GET /api/borrowing/{id}/` - تفاصيل إعارة
- `PUT /api/borrowing/{id}/return/` - إرجاع مصدر

#### التقارير
- `GET /api/reports/statistics/` - الإحصائيات العامة
- `GET /api/reports/borrowing-summary/` - ملخص الإعارات
- `GET /api/reports/user-activity/` - نشاط المستخدم

## أمثلة على الطلبات 📝

### تسجيل مستخدم جديد
```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "ahmed",
    "email": "ahmed@example.com",
    "password": "securepassword",
    "first_name": "أحمد",
    "last_name": "محمد"
  }'
```

### البحث عن كتب
```bash
curl -X GET "http://localhost:8000/api/resources/?search=الذكاء&category=العلوم" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### إعارة كتاب
```bash
curl -X POST http://localhost:8000/api/borrowing/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "resource_id": 1,
    "due_days": 14
  }'
```

## الاختبار 🧪

تشغيل جميع الاختبارات:
```bash
python manage.py test
```

تشغيل اختبارات تطبيق معين:
```bash
python manage.py test apps.resources
```

تشغيل اختبار محدد:
```bash
python manage.py test apps.borrowing.tests.BorrowingTestCase.test_borrow_resource
```

مع التقارير التفصيلية:
```bash
python manage.py test --verbosity=2
```

## النشر (Deployment) 🌐

### نشر على Heroku
```bash
# إنشاء تطبيق Heroku
heroku create your-app-name

# إضافة قاعدة بيانات
heroku addons:create heroku-postgresql:standard-0

# دفع الكود
git push heroku main

# تشغيل الترحيلات
heroku run python manage.py migrate
```

### نشر على Docker
```bash
# بناء الصورة
docker build -t erp-library-system .

# تشغيل الحاوية
docker run -p 8000:8000 erp-library-system
```

## الأمان 🔒

- استخدام HTTPS في الإنتاج
- تفعيل CORS بشكل صحيح
- استخدام متغيرات البيئة للحساسيات
- تطبيق نظام الأدوار والصلاحيات (RBAC)
- التحقق من المدخلات والتطهير
- استخدام JWT للمصادقة
- تفعيل CSRF Protection

## المساهمة 🤝

نرحب بمساهماتك! يرجى اتباع الخطوات التالية:

1. Fork المستودع
2. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add some amazing feature'`)
4. Push إلى الفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## معايير الكود

- اتبع PEP 8 لكود Python
- استخدم Type Hints
- اكتب اختبارات شاملة
- وثق الدوال والفئات بشكل صحيح
- استخدم رسائل commit واضحة

## حل المشاكل 🐛

### المشكلة: فشل الاتصال بقاعدة البيانات
```
الحل: تحقق من بيانات الاتصال في ملف .env
- تأكد من تشغيل PostgreSQL
- تحقق من صحة اسم المستخدم والكلمة السرية
```

### المشكلة: خطأ في الترحيلات
```
الحل: جرب الأوامر التالية
python manage.py migrate --fake-initial
python manage.py makemigrations --empty apps.resources --name fix
```

### المشكلة: مشاكل في الكاش
```
الحل: امسح الكاش
python manage.py clear_cache
# أو إذا كنت تستخدم Redis
redis-cli FLUSHALL
```

## السجل والإصدارات 📋

### الإصدار 1.0.0 (2025-12-23)
- ✅ إطلاق النسخة الأولى
- ✅ نظام إدارة المستخدمين الأساسي
- ✅ إدارة المصادر والمجموعات
- ✅ نظام الإعارة والعودة
- ✅ التقارير الأساسية
- ✅ الإشعارات البريدية

## المساعدة والدعم 💬

- 📧 البريد الإلكتروني: support@library-erp.com
- 💻 صفحة المشاكل: [GitHub Issues](https://github.com/AbdulRahman1052000/erp-library-system/issues)
- 📚 التوثيق الكاملة: [Documentation](https://docs.library-erp.com)
- 🌐 الموقع الرسمي: [www.library-erp.com](https://www.library-erp.com)

## الترخيص 📄

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## شكر وتقدير 🙏

شكر خاص لجميع المساهمين والمستخدمين الذين ساعدوا في تطوير هذا المشروع.

---

**آخر تحديث**: 23 ديسمبر 2025

تم إنشاء هذا المشروع بـ ❤️ بواسطة [AbdulRahman1052000](https://github.com/AbdulRahman1052000)
